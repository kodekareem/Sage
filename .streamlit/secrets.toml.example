# Copy this file to `.streamlit/secrets.toml` and fill in a key to enable the
# optional "claude" engine. The app works fine WITHOUT this — it defaults to the
# free, no-key "rule" engine.
#
# ANTHROPIC_API_KEY = "sk-ant-..."
